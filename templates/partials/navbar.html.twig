<nav class="navbar navbar-expand-lg sticky-top py-0 navbar-dark bg-primary" id="mainNav">
    <div class="container-fluid">
        <a class="navbar-brand text-white" href="{{ path('home') }}">Seekriders</a>
        <button class="navbar-toggler text-white" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            <i class="fas fa-ellipsis-v"></i>
        </button>
        <div class="collapse navbar-collapse navbar-dark bg-primary" id="navbarResponsive">
            <ul class="navbar-nav ml-auto">
                {% if app.user %}
                    <!-- dropdown account -->
                    <li class="nav-item dropdown">
                        <a href="#" class="nav-link dropdown-toggle text-white" id="accountDropdownLink" data-toggle="dropdown">
                            {% if app.user.avatar %}
                                <img src="{{ vich_uploader_asset(app.user, 'avatarFile') | imagine_filter('avatarMin') }}" class="avatar rounded-circle img-fluid" alt="Avatar de {{ app.user.username }}">
                            {% else %}
                                <img src="{{ asset('images/users/profile-image-null.png') | imagine_filter('avatarMin') }}" class="avatar rounded-circle img-fluid" alt="Avatar de {{ app.user.username }}">
                            {% endif %}
                        </a>
                        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                            <a href="{{ path('account_index') }}" class="dropdown-item">mon profile</a>
                            <a href="{{ path('account_profile') }}" class="dropdown-item">modifier mon profile</a>
                            <a href="{{ path('account_password') }}" class="dropdown-item">Modifier mon mot de passe</a>
                            <div class="dropdown-divider"></div>
                            {% if is_granted('ROLE_ADMIN') %}
                                <a href="{{ path('admin_dashboard') }}" class="dropdown-item">Administration</a>
                                <div class="dropdown-divider"></div>
                            {% endif %}
                            <a href="{{ path('account_logout') }}" class="dropdown-item">Se déconnecter</a>
                        </div>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('account_login') }}">Se connecter</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('account_register') }}">S'enregistrer</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
